# Mount the model-proxy dispatcher patch into a DSH profile.
# Config (可选)：proxy、noProxy；默认读标准 proxy 环境变量，最终回退
# http://127.0.0.1:10808。Web 设置页「插件 → 插件配置 → 模型代理」可实时
# 修改。只改传输层，不改模型 API endpoint。
- insert:
    - id: dsh-model-proxy
      name: 'dsh-model-proxy'
