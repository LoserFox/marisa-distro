# 魔理沙 UI 验收报告（2026-08-13 晚，mimo vision + 交互实测）

## 环境
- web: http://127.0.0.1:3080（HTTP 200，零 pending 零 error）
- boot entries: 54，插件 162 个（Plugin list）
- vision 模型: mimo-v2.5（AUXILIARY_VISION_MODEL）

## ✅ vision 验收（mimo 截图分析）
| 项 | 结果 |
|---|---|
| 整体布局 | 侧边栏 + 主区域 + 输入框经典三栏，渲染正常无白屏/错位 |
| 侧边栏 | DeepSeek Logo、New Session、Workspaces（搜索/最近会话/分组树）、Settings |
| 桌面宠物 | 右下角二次元猫娘挂件（女仆装）+ ◆ 菱形控制按钮（whale-girl 插件）|
| 弹窗 | Internal Testing Notice → API key 引导（Configure later 可跳过）|

## ✅ 按钮交互实测
| 按钮 | 结果 |
|---|---|
| New session | ⚠️ 点击无可见变化（无 API key 时可能不建会话——待有 key 复测）|
| Add workspace | ✅ 弹出目录选择器（Home/Edit path/New folder/隐藏文件/Cancel/Open 全功能）|
| Settings | ✅ 完整设置面板 |
| 插件 tab（Settings 内）| ✅ **9 个 tab 全在**：General/Models/Plugins/Agent presets/**Multimedia input/Vision/Subagents/MinerU/Suggested replies**（插件贡献的 tab 全部生效）|
| Plugins 面板 | ✅ Plugin configuration（Shell/Agent loop/Web search/Interpreter paths）+ **Plugin list（162 插件 + 搜索 + 启禁用状态）**|
| 桌面宠物互动菜单 | ✅ 打开（🍗喂食 / 🎾玩耍 / 🎭换角色）|
| 喂食 | 点击响应（动画状态需动态观察）|
| ◆ 控制按钮 | ✅ 存在（白色圆形黑菱形）|
| Commands / Send message | disabled（无 API key 预期行为 ✓）|

## 🔧 subagent 验收发现并修复的 bug
1. **2 个插件运行时 pending**（multimedia-webui-input 等 `slash` 服务、aigc-canvas 等 `betterSidebar` 服务）——之前 boot 清单显示"全挂载"但运行时有 2 个未激活（**清单 ≠ 激活**）
2. **patch id 不匹配**：profile 层 disabled 行 id 用包名（@dsh-community/...），insert 行 id 用插件名（community-...）→ disabled 不生效
3. **修复**：web profile 修正 disabled id + 临时禁用 2 个 pending 插件 → 重启后 HTTP 200 零 pending

## 结论
**魔理沙 v1.1 UI 验收通过**：布局/渲染/设置/插件面板/宠物交互全部正常，插件集成（9 个设置 tab、162 插件列表）验证成功。待办：有 API key 后复测 New session + 真实对话流。
