# The rc7 web-app base bundle provides the input stack (ui-input-trigger +
# ui-commands); these rows assert them enabled for the standalone WebView.
- id: ui-input-trigger
  name: '@deepseek-ai/dsh-client-ui-input-trigger'
  disabled: false
- id: ui-commands
  name: '@deepseek-ai/dsh-client-ui-commands'
  disabled: false
