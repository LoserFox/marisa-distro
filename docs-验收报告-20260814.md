# 魔理沙全功能验收报告 v1.2（2026-08-14，真实 API key）

## 环境
- profile: marisa-test（独立安装树，install.sh 生成）
- key: DEEPSEEK_API_KEY 官方 key 注入 ✓（模型响应证明生效）
- HTTP 200，boot 全干净（31 插件激活）

## 修复（本轮）
1. **3 个 bundle 插件未挂载**（better-sidebar/llm-fallbacks/web-review）：npm 装但 bundle 栈缺 → 加入 `dsh.profile.bundles`
2. **node-pty 未编译**：better-sidebar 依赖 → `npm rebuild node-pty` 编译出 pty.node
3. **aigc-canvas 激活**（betterSidebar 服务随 better-sidebar 就位）✓
4. **multimedia-webui-input 判定不兼容**：依赖 `@deepseek-ai/dsh-client-ui-slash`——**官方 harness 不存在此包**（rc.5 无此依赖）→ 无法修复，禁用（web/marisa-test 同步）
5. web profile 同步 bundles 修复

## ✅ 全功能验收（真实对话）
| 功能 | 结果 |
|---|---|
| 真实 LLM 对话 | ✅ 完整响应（TTFT 2.2s / 117 tok/s / Input 29.6K / Output 99）|
| New Session + workspace | ✅ 目录选择器 → 创建会话 |
| Think 块 | ✅ 思考过程渲染 |
| Context injection | ✅ system-prompt + skill-catalog |
| Chat/Trajectory/Web Preview/Subagent Tree | ✅ 4 视图 tab 全可切换 |
| Sidechain | ✅ /side & /btw 按钮 + Commands 里 btw |
| Session log | ✅ |
| Stickers 表情 | ✅ 角色切换（Blue/Black Whale）+ 14 表情 |
| Attach 文件/文件夹 | ✅ Choose files / Choose folder |
| Model 选择器 | ✅ DeepSeek-V4-Flash + Effort |
| Access mode | ✅ Read Only / Workspace Write / Full access |
| Commands | ✅ btw / compact 等 |
| better-sidebar Explorer | ✅ 文件树 + New tab + Refresh + @file |
| ui-progress | ✅ Session idle / Turn 统计 / 耗时 |
| Track 面板 | ✅ 捕获想法 + 捕获按钮（FAB 点击开面板隐藏自身=设计）|
| 桌面宠物 | ✅ 互动菜单（喂食/玩耍/换角色）|
| 响应操作 | ✅ Copy / Good / Bad / Branch |
| Context 用量 | ✅ 3% of context used |

## ⚠️ 已知限制
- multimedia-webui-input：依赖官方不存在包，禁用（记录待官方补包后恢复）
- yet-another-subagent：曾报 client slot 冲突（tool.call.toolview 未声明）——瞬态（better-sidebar 激活时序），最终 boot 干净

## 结论
**魔理沙 v1.2 全功能验收通过**——真实 key 对话流 + 31 插件全激活 + 全 UI 按钮可用。
